{% extends "base.html.twig" %}

{% block title "Ajout de recette "%}
    
{% block body %}
    <h1>Ajout d'une nouvelle recette</h1>

    {{ form_start(form) }}

    <div class="d-flex">
        {{ form_row(form.title) }}
        {{ form_row(form.duration) }}

    </div>
    {{ form_rest(form) }}
    {{ form_end(form) }}

{% endblock body %}